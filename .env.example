# OAuth Configuration (for Todoist OAuth)
CLIENT_ID=your_oauth_client_id
CLIENT_SECRET=your_oauth_client_secret
VERIFICATION_TOKEN=your_verification_token

# Server Configuration (for remote deployment)
SERVER_HOST=0.0.0.0
SERVER_PORT=8765

# Base URL for the remote server (used in integration URLs)
# This should be your public domain with wss:// protocol
BASE_URL=wss://mcp-todoist.yourdomain.com